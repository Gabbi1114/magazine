# Backend server URL (Render.com)
VITE_API_URL=https://your-backend.onrender.com

# CDN base URL (Cloudflare R2 public domain or custom domain)
# Used to detect which URLs are R2 CDN URLs so photos can be deleted
VITE_CDN_URL=https://pub-xxxx.r2.dev
